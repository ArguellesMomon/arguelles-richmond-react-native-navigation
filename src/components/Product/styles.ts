import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    padding: 12,
    marginVertical: 6,
    backgroundColor: "#eee",
    borderRadius: 8
  },
  name: {
    fontSize: 16,
    fontWeight: "bold"
  },
  price: {
    fontSize: 14,
    marginTop: 4
  }
});
