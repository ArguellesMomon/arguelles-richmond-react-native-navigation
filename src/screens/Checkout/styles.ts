import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16
  },
  total: {
    fontSize: 18,
    fontWeight: "bold",
    marginVertical: 16
  }
});
